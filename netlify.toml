# Netlify configuration
# On each deploy, build.js scans assets/work/<category>/ and writes work.json
# listing EVERY file — so all your videos show, reliably, in order.

[build]
  command = "node build.js"
  publish = "."
