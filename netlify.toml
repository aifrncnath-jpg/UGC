# Netlify configuration
# This is a plain static site with no build step. Just publish the folder.
# The Work gallery auto-detects files you drop into assets/work/<category>/.

[build]
  publish = "."
